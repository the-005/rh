import styles from "./style.module.css";

/**
 * About: the third view, and the only one that will be words rather than
 * pictures. Same paper as the index (white, the site's sans, the frame's
 * difference blend reading black over it). Empty for now: the placeholder copy
 * was removed and the real text hasn't arrived. The earlier statement column and
 * contact / studio / services / clients rail are in git history if they're
 * useful as a starting point.
 */
export function AboutPage() {
  return <main className={styles.page} />;
}
